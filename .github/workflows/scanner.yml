name: Scanner Crypto ETH/BTC Focus

on:
  schedule:
    # Executa a cada 15 minutos durante horário de trading
    - cron: '*/15 6-23 * * *'
  workflow_dispatch: {}  # Permite execução manual

# Evita execuções simultâneas se um job atrasar
concurrency:
  group: scanner-crypto
  cancel-in-progress: false

permissions:
  contents: write  # 🔧 ALTERADO: read → write para permitir commits

jobs:
  crypto-scanner:
    runs-on: ubuntu-latest
    timeout-minutes: 12

    env:
      # Ajusta timezone para logs coerentes com Brasília
      TZ: America/Sao_Paulo

      # (Opcional) Secrets usados pelo script
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      FMP_API_KEY: ${{ secrets.FMP_API_KEY }}

      # ====== FLAGS (ligar/desligar recursos do main.py) ======
      ATIVAR_MACRO_UNICO: "true"
      ATIVAR_SCORE_COMPONENTES: "true"
      ATIVAR_VWAP: "true"
      ATIVAR_BBWIDTH: "true"
      ATIVAR_FILTRO_LIQUIDEZ: "false"
      LIQ_MINIMO_30D: "0"

      # Pesos do score 0–100
      PESO_TENDENCIA: "1.0"
      PESO_MOMENTO: "1.0"
      PESO_VOLUME: "1.0"
      PESO_VOLATILIDADE: "1.0"
      PESO_CONFLUENCIA: "1.0"

    steps:
      - name: 🏁 Checkout Repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0  # Necessário para commits

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies (requirements.txt)
        run: |
          python -m pip install --upgrade pip setuptools wheel
          if [ -f requirements.txt ]; then
            pip install --no-cache-dir -r requirements.txt
          else
            echo "❌ requirements.txt não encontrado na raiz do repositório."
            exit 1
          fi

      - name: 🔎 Show versions
        run: |
          python - << 'PY'
          import sys, importlib
          print("Python:", sys.version)
          for m in ("ccxt","pandas","ta","pandas_ta","requests"):
              try:
                  mod = importlib.import_module(m)
                  ver = getattr(mod, "__version__", "n/a")
                  print(f"{m}: {ver}")
              except Exception as e:
                  print(f"{m}: ❌ ({e})")
          PY

      - name: ✅ Verify Installation
        run: |
          python - << 'PY'
          import sys
          required_modules = ['pandas', 'numpy', 'ccxt', 'ta', 'requests']
          ok = True
          for module in required_modules:
              try:
                  __import__(module)
                  print(f'✅ {module}: OK')
              except Exception as e:
                  print(f'❌ {module}: FAILED - {e}')
                  ok = False
          sys.exit(0 if ok else 1)
          PY

      - name: 🧰 System & Resources
        run: |
          python --version
          pip --version
          echo ""
          echo "📂 Workspace:"
          ls -lah
          echo ""
          echo "💾 Espaço em disco:"
          df -h
          echo ""
          echo "🧠 Memória:"
          free -h

      - name: ▶️ Run Scanner
        run: |
          set -e
          # Detecta arquivo de entrada (preferência: main.py; fallback: scanner.py)
          if [ -f main.py ]; then EP=main.py;
          elif [ -f scanner.py ]; then EP=scanner.py;
          else echo "❌ Nenhum entrypoint encontrado (main.py/scanner.py)."; exit 1; fi
          echo "Entrypoint: $EP"
          echo "🔧 Flags -> MACRO=$ATIVAR_MACRO_UNICO SCORE=$ATIVAR_SCORE_COMPONENTES VWAP=$ATIVAR_VWAP BBWIDTH=$ATIVAR_BBWIDTH LIQ=$ATIVAR_FILTRO_LIQUIDEZ MIN_LIQ=$LIQ_MINIMO_30D"
          echo "🔧 Pesos  -> TEND=$PESO_TENDENCIA MOM=$PESO_MOMENTO VOL=$PESO_VOLUME VOLAT=$PESO_VOLATILIDADE CONF=$PESO_CONFLUENCIA"
          python "$EP"

      # 🆕 NOVO STEP: Commit automático de dados persistentes
      - name: 💾 Commit dados persistentes
        if: success()
        run: |
          # Configurar git
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "GitHub Actions Bot"
          
          # Criar diretórios se não existirem
          mkdir -p data logs
          
          # Adicionar arquivos de dados (forçar inclusão mesmo se no .gitignore)
          git add -f data/ledger_sinais.csv 2>/dev/null || echo "⚠️ Ledger não encontrado"
          git add -f data/throttle.json 2>/dev/null || echo "⚠️ Throttle não encontrado"
          git add -f logs/scanner_*.log 2>/dev/null || echo "⚠️ Logs não encontrados"
          
          # Verificar se há mudanças para commitar
          if git diff --staged --quiet; then
            echo "✅ Nenhuma mudança nos dados para commit"
          else
            # Fazer commit
            TIMESTAMP=$(date -u +'%Y-%m-%d %H:%M:%S UTC')
            SINAIS_COUNT=$(wc -l < data/ledger_sinais.csv 2>/dev/null | tr -d ' ' || echo "0")
            
            git commit -m "📊 Auto-update dados [${TIMESTAMP}]

- Ledger: ${SINAIS_COUNT} sinais registrados
- Throttle atualizado
- Logs persistidos

[skip ci]"
            
            # Push para o repositório
            git push
            echo "✅ Dados persistidos com sucesso!"
          fi

      - name: 📤 Upload Artifacts (logs, relatórios)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scanner-artifacts-${{ github.run_number }}
          path: |
            logs/**
            data/**
            output/**
            reports/**
          if-no-files-found: ignore
          retention-days: 7

      - name: 🧹 Cleanup on Failure
        if: failure()
        run: |
          pip cache purge || true
          find . -name "*.pyc" -delete || true
          find . -name "__pycache__" -type d -exec rm -rf {} + || true
